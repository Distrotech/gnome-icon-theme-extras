This repository is meant to be the source for artists to better cooperate and create derivate works. It doesn't include icons in the output format (PNG), but only the source images (Inkscape SVG, GIMP's XCF).
